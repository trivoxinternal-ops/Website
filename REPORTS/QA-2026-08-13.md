# TRIVOX QA Report — 2026-08-13

**Run date:** 2026-08-13  
**Pages audited (in repo):** index.html, ai-automation-toronto.html, ai-automation-gta.html, ai-automation-mississauga.html, ai-voice-agents.html, ai-receptionist.html, workflow-automation.html, case-car-deals-direct.html, case-clinic-receptionist.html, clinic-chatbot.html, privacy.html (11 total)  
**Overall status:** ⚠️ 4 issues (2 critical, 2 minor)

---

## ❌ CRITICAL: Sitemap declares 334 pages that don't exist in repo

`sitemap.xml` lists **345 URLs**. Only **10** have corresponding files in the repo. The remaining **334** are either:
- 404ing on the live site right now (SEO catastrophe — crawlers will find 334 dead ends), or  
- Deployed separately from this repo (if so, they are not tracked in version control).

**Action required:** Verify on the live server whether these pages actually serve content. If they 404, either remove them from the sitemap or push the missing HTML files. Affected categories include:
- `get-recommended-by-chatgpt.html`, `what-is-geo.html` (both also linked from 4 pages in the repo — see Issue 2)
- ~50+ `ai-visibility-for-*.html` pages (dentists, plumbers, lawyers, med-spas, roofing, etc.)
- ~50+ `ai-visibility-for-*-{city}.html` location variants
- ~50+ `{industry}-marketing-{city}.html` pages
- `industries.html`, `guides.html`, `locations.html`
- GEO/AI-search articles: `how-to-show-up-in-google-ai-overviews.html`, `what-is-geo.html`, `is-seo-dead.html`, `how-to-rank-on-perplexity.html`, etc.

---

## ❌ CRITICAL: 2 pages linked from 4 live pages return 404

Two pages are referenced by live, indexed pages but **the files do not exist in the repo**:

| Missing page | Linked from |
|---|---|
| `get-recommended-by-chatgpt.html` | ai-automation-toronto.html:244, ai-automation-gta.html:263, ai-automation-mississauga.html:232, ai-receptionist.html:218 |
| `what-is-geo.html` | ai-automation-toronto.html:245, ai-automation-gta.html:264, ai-automation-mississauga.html:233, ai-receptionist.html:218 |

Users clicking "Get Recommended by ChatGPT" or "What is GEO?" from these pages hit a 404. These are also in sitemap.xml (lastmod 2026-08-03), meaning Google is actively trying to crawl them.

**Fix:** Push the two missing HTML files, or temporarily remove the links until the pages are ready.

---

## ⚠️ MINOR: `clinic-chatbot.html` missing SEO meta + not noindexed

`clinic-chatbot.html` is a standalone client demo widget ("Northview Psychology — AI Receptionist") that is:
- In the sitemap at priority 0.6 (Google will crawl it)
- Missing: `<meta name="description">`, `<link rel="canonical">`, `og:title`, `og:description`, `og:image`, `twitter:card`
- Has no `<h1>`

This page appears to be a client-facing demo embed, not a TRIVOX content page. If it shouldn't rank in Google, add `<meta name="robots" content="noindex">` and remove from sitemap.

---

## ⚠️ MINOR: `privacy.html` missing SEO meta

`privacy.html` is missing: `<meta name="description">`, `<link rel="canonical">`, `og:title`, `og:description`, `og:image`, `twitter:card`. Low priority since privacy pages rarely drive traffic, but a canonical prevents duplicate-content issues if the page is ever linked with query strings.

---

## ✅ CLEAN — Broken/Missing Local Assets

All local asset references verified against repo:
- `ds/trivox-icon.png` ✅  
- `ds/sign.jpg` ✅  
- `ds/motion.css`, `ds/motion.js`, `ds/site.css`, `ds/tokens.css`, `ds/v2.css`, `ds/viz.js`, `ds/scroll.js` ✅  
- `assets/favicon.png`, `assets/apple-touch-icon.png` ✅  
- `assets/og-image.png` ✅ (used as og:image on all 9 main pages)

No broken local asset references found.

---

## ✅ CLEAN — SEO / Social Meta (9 main pages)

All 9 primary landing pages (index, ai-automation-toronto, ai-automation-gta, ai-automation-mississauga, ai-voice-agents, ai-receptionist, workflow-automation, case-car-deals-direct, case-clinic-receptionist) have:

- `<title>` non-empty ✅  
- `<meta name="description">` ✅  
- `<link rel="canonical">` ✅  
- `og:title`, `og:description`, `og:image` (assets/og-image.png — file exists) ✅  
- `twitter:card`, `twitter:image` ✅  
- `<meta name="viewport">` ✅  
- Favicon link ✅  

---

## ✅ CLEAN — Accessibility

- All `<img>` tags have non-empty `alt` attributes ✅  
- All pages have `<html lang="en">` ✅  
- All 9 primary pages have exactly one `<h1>` ✅  
- Heading structure (h1→h2→h3) is consistent with no skipped levels ✅  

---

## ✅ CLEAN — Hidden Content / Mobile Bugs

`[data-reveal]` elements use `opacity:0` + IntersectionObserver with **threshold: 0.15** (15%) and `rootMargin: '0px 0px -8% 0px'` — this is a low threshold that is unlikely to cause elements to stay hidden on mobile.

GSAP ScrollTrigger reveals (`start: '82%'` for section heads, `'85%'` for products, `'70%'` for vault) use GSAP `from()` (not opacity:0 baseline), so they cannot get permanently stuck invisible.

No dangerously high thresholds (≥ 0.4) found. ✅

---

## ✅ CLEAN — Forms

No `<form>` elements exist on any page — all CTAs point to external services (Calendly, Stripe). The `<input>` in `clinic-chatbot.html` is a JS-driven chat widget (no `<form>` wrapper) and is not a data-submission form. ✅

---

## ✅ CLEAN — Internal Anchor Links

All `href="#section"` anchors in `index.html` resolve to matching `id` attributes on the same page:
- `#services` → `id="services"` ✅  
- `#work` → `id="work"` ✅  
- `#store` → `id="store"` ✅  
- `#about` → `id="about"` ✅  
- `#faq` → `id="faq"` ✅  

All other pages link to `index.html#services`, `index.html#work`, etc. — cross-page anchors, valid. ✅  
`href="#"` on "Mon–Fri, 9–6 EST" is intentional (no-op link) — acceptable. ✅

---

## Cognizance note

Cognizance: source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).

---

## Liveness

Liveness: not checked (remote egress blocked).

---

*Generated by TRIVOX QA Agent · 2026-08-13*
