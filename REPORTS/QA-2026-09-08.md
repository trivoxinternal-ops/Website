# TRIVOX QA Report — 2026-09-08

**Audited:** 2026-09-08 | **Pages:** 11 HTML files | **Overall:** ⚠️ 4 issues (2 high, 2 low)

---

## Executive Summary

Two missing HTML pages are generating live 404s — linked from 5 pages each. All local assets (CSS, JS, images) exist. SEO is solid on all public landing pages. Accessibility is clean. The IntersectionObserver reveal system is well-configured (threshold 0.15 with proper fallbacks — no mobile hidden-content risk).

---

## 1. Broken / Missing Asset References

### 🔴 HIGH — `get-recommended-by-chatgpt.html` does not exist in repo
- **Effect:** Live 404 on every page that links to it.
- **Linked from (5 pages):** `index.html`, `ai-automation-gta.html`, `ai-automation-toronto.html`, `ai-automation-mississauga.html`, `ai-receptionist.html`
- **Action:** Create the page or remove/update the links.

### 🔴 HIGH — `what-is-geo.html` does not exist in repo
- **Effect:** Live 404 on every page that links to it.
- **Linked from (5 pages):** `index.html`, `ai-automation-gta.html`, `ai-automation-toronto.html`, `ai-automation-mississauga.html`, `ai-receptionist.html`
- **Action:** Create the page or remove/update the links.

### ✅ All local assets exist
CSS (`ds/tokens.css`, `ds/site.css`, `ds/motion.css`, `ds/v2.css`), JS (`ds/motion.js`, `ds/scroll.js`, `ds/viz.js`), images (`ds/sign.jpg`, `ds/trivox-icon.png`, `assets/og-image.png`, `assets/favicon.png`, `assets/apple-touch-icon.png`, `assets/favicon-192.png`) — all present.

---

## 2. SEO / Social Meta

### ✅ All public landing pages — clean
`index.html`, `ai-automation-toronto.html`, `ai-automation-mississauga.html`, `ai-automation-gta.html`, `ai-receptionist.html`, `ai-voice-agents.html`, `case-clinic-receptionist.html`, `case-car-deals-direct.html`, `workflow-automation.html` all have: `<title>`, `meta description`, `canonical`, `og:title`, `og:description`, `og:image` (https://trivox.ca/assets/og-image.png — file exists ✅), `twitter:card`, `twitter:image`, `viewport`, favicon.

### ⚠️ LOW — `privacy.html` missing social/canonical meta
- Missing: `canonical`, `og:title`, `og:description`, `og:image`, `twitter:card`, `twitter:image`
- Has: `<title>`, `meta charset`, `viewport`, favicon
- Not critical (privacy pages rarely get shared), but canonical is recommended.

### ⚠️ LOW — `clinic-chatbot.html` is a client demo page with no public SEO meta
- Missing all: canonical, og:*, twitter:*, meta description
- Has: `<title>Northview Psychology — AI Receptionist</title>`, charset, viewport, favicon
- **Likely intentional** (internal/client-facing demo page, not a public SEO page). No action needed unless this page is to be publicly indexed.

---

## 3. Accessibility

### ✅ Clean
- All `<img>` tags have non-empty `alt` attributes.
- All pages have `<html lang="en">`.
- Every public page has exactly **1 `<h1>`**.
- `clinic-chatbot.html` has no `<h1>` — acceptable for a demo widget page.
- No skipped heading levels found.

---

## 4. Hidden Content / Mobile Bugs

### ✅ Clean — reveal system is safe
- `[data-reveal]` starts at `opacity:0; transform:translateY(26px)` (motion.css).
- `motion.js` uses `IntersectionObserver` with **threshold: 0.15** and `rootMargin: '0px 0px -8% 0px'`.
  - Threshold 0.15 is safe for mobile — only 15% of element must be visible to trigger reveal. No tall-element-stuck-hidden risk.
- **Fallbacks implemented correctly:**
  1. `prefers-reduced-motion` → immediately adds `.in` to all reveal elements.
  2. No `IntersectionObserver` support → adds `.in` to all elements.
- `@media print` / reduced-motion CSS also sets `opacity:1 !important` as a safety net.
- **No mobile hidden-content risk detected.**

---

## 5. Forms

### ✅ Clean
No contact forms found on any public page. The only `<input>` is the chat widget in `clinic-chatbot.html` (no `name` attribute needed for a JS-driven chat input). No `method`/`action`/`data-netlify` issues.

---

## 6. Internal Anchor Links

### ✅ index.html — all anchors resolve
Anchors used: `#about`, `#faq`, `#services`, `#store`, `#work`  
IDs defined: `about`, `faq`, `services`, `store`, `work`, `gl`, `v2`, `vg` — all anchors matched. ✅

### ✅ Subpages — no internal anchors used in navigation (clean)

### ✅ tel:/mailto: not found in raw markup (links go through external integrations)

---

## 7. Liveness

Liveness: not checked (remote egress blocked — source audit is the deliverable).

---

## Cognizance

Cognizance: source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).

---

## Action Items

| Priority | Issue | Pages Affected |
|---|---|---|
| 🔴 HIGH | Create `get-recommended-by-chatgpt.html` or remove its links | 5 pages |
| 🔴 HIGH | Create `what-is-geo.html` or remove its links | 5 pages |
| ⚠️ LOW | Add `canonical` tag to `privacy.html` | 1 page |
| ⚠️ LOW | Decide if `clinic-chatbot.html` needs public SEO meta | 1 page |

---

*Generated by TRIVOX QA Agent — automated daily audit*
