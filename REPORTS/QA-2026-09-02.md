# TRIVOX QA — Daily Audit: 2026-09-02

**Pages audited:** 11 HTML files (`index.html`, `ai-automation-gta.html`, `ai-automation-mississauga.html`, `ai-automation-toronto.html`, `ai-receptionist.html`, `ai-voice-agents.html`, `case-car-deals-direct.html`, `case-clinic-receptionist.html`, `clinic-chatbot.html`, `privacy.html`, `workflow-automation.html`)

**Overall status: ⚠️ 2 issues found (1 high, 1 low)**

---

## 1. Broken / Missing Asset References

### ❌ HIGH — Two linked pages do not exist in the repo (404 on live site)

| Missing file | Pages that link to it |
|---|---|
| `get-recommended-by-chatgpt.html` | `index.html`, `ai-automation-gta.html`, `ai-automation-mississauga.html`, `ai-automation-toronto.html`, `ai-receptionist.html` |
| `what-is-geo.html` | `index.html`, `ai-automation-gta.html`, `ai-automation-mississauga.html`, `ai-automation-toronto.html`, `ai-receptionist.html` |

These are nav/footer links on 5 pages. Clicking them delivers a 404. Either the pages need to be created or the links need to be removed/redirected.

### ✅ All other local assets present

Checked: `ds/trivox-icon.png`, `ds/sign.jpg`, `ds/motion.js`, `ds/viz.js`, `ds/scroll.js`, `ds/tokens.css`, `ds/motion.css`, `ds/site.css`, `ds/v2.css`, `assets/favicon.png`, `assets/apple-touch-icon.png`, `assets/og-image.png`. All exist.

External CDN assets (GSAP 3.12.5, Google Fonts) — not verified for liveness but URLs are well-formed.

---

## 2. SEO / Social Meta

### ⚠️ LOW — `clinic-chatbot.html` has no SEO meta

This page (Northview Psychology AI Receptionist demo) is missing: `<meta name="description">`, `<link rel="canonical">`, `og:title`, `og:description`, `og:image`, `twitter:card`. Since it appears to be a client-facing embedded chatbot page (not indexed in the TRIVOX sitemap), this may be intentional. If it should not be indexed, add `<meta name="robots" content="noindex">`. If it should be, add full meta.

### ⚠️ LOW — `privacy.html` has no meta description or OG tags

No description, no canonical, no OG/Twitter meta. Low priority for a privacy page but worth adding for completeness.

### ✅ All primary service/landing pages — clean

All 9 main pages have: `<title>` ✅ | `meta description` ✅ | `canonical` ✅ | `og:title` ✅ | `og:description` ✅ | `og:image` ✅ | `twitter:card` ✅ | `viewport` ✅ | `favicon` ✅

OG image `https://trivox.ca/assets/og-image.png` confirmed present in repo.

---

## 3. Accessibility

### ⚠️ LOW — `clinic-chatbot.html` has no `<h1>`

This is a full-page chat widget; no headings at all. It is a standalone embedded tool (not a standard content page), so this may be intentional, but semantically incorrect.

### ✅ All other pages — clean

- `<html lang="en">` present on all pages ✅
- Exactly one `<h1>` per page (all except clinic-chatbot) ✅
- No heading levels skipped on main pages ✅
- All `<img>` tags have non-empty `alt` attributes ✅

---

## 4. Hidden Content / Mobile Animation Bugs

### ✅ Clean — animation system is safe

- `motion.js` uses `IntersectionObserver` with `threshold: 0.15` (15%) — well below the 0.4 danger threshold. Safe on mobile.
- `rootMargin: '0px 0px -8% 0px'` — mild bottom offset, not aggressive.
- `motion.css` contains `@media (prefers-reduced-motion)` override: forces all `[data-reveal]` to `opacity:1 !important` — so no content can stay hidden if animations are disabled.
- `scroll.js` (GSAP ScrollTrigger) uses **transform-only** animations (no opacity changes), explicitly designed so content can never get stuck invisible.
- `index.html` inline GSAP uses `start: 'top 70%-88%'` triggers — reasonable on mobile.

All reveal elements have a working fallback. No mobile visibility bugs detected.

---

## 5. Forms

### ✅ Clean — no forms on the site

No `<form>` elements found across any page. This is a marketing/agency site using mailto CTAs. No issues.

---

## 6. Internal Links

### ❌ Already reported in §1 — see missing pages above

### ✅ In-page anchor links — clean (index.html)

Anchors used: `#services`, `#work`, `#about`, `#faq`, `#store` — all have matching `id=` attributes in `index.html`.

### ✅ Tel/Mailto links — well-formed

- `mailto:daniel@trivox.ca` ✅
- `mailto:info@trivox.ca` ✅
- Long mailto with subject+body params ✅

---

## Cognizance

Cognizance: source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).

---

## Liveness

Liveness: not checked (remote egress blocked).

---

*Generated by TRIVOX QA Agent — 2026-09-02*
