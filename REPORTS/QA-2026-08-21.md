# TRIVOX QA Report — 2026-08-21

**Pages audited:** index.html, ai-automation-toronto.html, ai-automation-gta.html, ai-automation-mississauga.html, ai-voice-agents.html, ai-receptionist.html, clinic-chatbot.html, case-clinic-receptionist.html, case-car-deals-direct.html, workflow-automation.html, privacy.html (11 pages)  
**Overall status:** ⚠️ 3 real issues found

---

## 1. Broken Internal Links — CRITICAL

### 1a. Two planned pages do not exist yet (live 404s)
`get-recommended-by-chatgpt.html` and `what-is-geo.html` are linked from **5 live pages** but neither file exists in the repo, producing 404 errors on the live site. This also breaks the internal linking of the SEO cluster.

Pages with dangling links:
- `index.html` (FAQ section)
- `ai-automation-toronto.html` (lines 244–245)
- `ai-automation-gta.html` (lines 263–264)
- `ai-automation-mississauga.html` (lines 232–233)
- `ai-receptionist.html` (line 218)

**Fix:** Either create both pages or change the links to `#` / a placeholder until the pages are ready.

---

## 2. Stale Booking URL — MODERATE

`workflow-automation.html` and `case-car-deals-direct.html` have **10+ CTAs** pointing to the old **Calendly** link:  
`https://calendly.com/trivoxinternal/intro-call`

Every other page (index, ai-automation-toronto, ai-voice-agents, etc.) correctly uses:  
`https://cal.com/trivox-interno-hfbm3x/trivox-discovery-call`

Visitors clicking "Book a call" or "Book a free diagnostic" on these two pages land on a broken or outdated booking flow.

**Fix:** Replace all `calendly.com/trivoxinternal/intro-call` with `cal.com/trivox-interno-hfbm3x/trivox-discovery-call` in both files.

---

## 3. LinkedIn URL Inconsistency — MINOR

Three pages link to `https://www.linkedin.com/in/trivoxai` (personal profile) in their footer:
- `ai-automation-toronto.html`
- `ai-voice-agents.html`
- `workflow-automation.html`

`index.html` correctly links to `https://www.linkedin.com/company/trivox-ai` (company page).

**Fix:** Standardize all footer LinkedIn links to the company page URL.

---

## Assets — ✅ Clean

All local asset references verified present in repo:
- `assets/og-image.png` ✅
- `assets/favicon.png` ✅
- `assets/apple-touch-icon.png` ✅
- `ds/trivox-icon.png` ✅
- `ds/sign.jpg` ✅
- `ds/motion.js`, `ds/motion.css`, `ds/site.css`, `ds/tokens.css`, `ds/v2.css`, `ds/viz.js`, `ds/scroll.js` ✅

---

## SEO / Social Meta — ✅ Clean (except clinic-chatbot.html)

All marketing pages have `<title>`, `<meta description>`, canonical, og:title/description/image, twitter:card/image, viewport, and favicon.  
`og:image` → `assets/og-image.png` exists ✅

**Exception:** `clinic-chatbot.html` has no meta description, no canonical, no og:/twitter: tags, and no `robots: noindex`. It will be crawled. This is a demo page; consider adding `<meta name="robots" content="noindex, nofollow">` to keep it out of search results.

---

## Accessibility — ✅ Clean

- `<html lang="en">` present on all pages.
- One `<h1>` per marketing page.
- All `<img>` tags have non-empty `alt` attributes.
- `[data-reveal]` IntersectionObserver threshold is **0.15** (15%) — safe for mobile; no tall-element stuck-hidden risk.

---

## Forms — N/A

No `<form>` elements found on any page.

---

## Anchor Links (index.html) — ✅ Clean

- `#services` → `<section id="services">` ✅
- `#work` → `<section id="work">` ✅
- `#store` → `<section id="store">` ✅
- `#about` → `<section id="about">` ✅
- `#faq` → `<section id="faq">` ✅

---

## Liveness

Not checked (remote egress blocked).

---

## Cognizance

Source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).
