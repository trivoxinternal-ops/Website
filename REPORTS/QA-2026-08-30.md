# TRIVOX QA — Daily Audit 2026-08-30

**Auditor:** TRIVOX AI QA Agent  
**Pages audited:** index.html, ai-automation-toronto.html, ai-automation-gta.html, ai-automation-mississauga.html, ai-receptionist.html, ai-voice-agents.html, workflow-automation.html, case-clinic-receptionist.html, case-car-deals-direct.html, clinic-chatbot.html, privacy.html (11 pages)  
**Overall status:** ⚠️ 3 issues (1 critical SEO, 2 medium)  
**Liveness:** not checked (remote egress blocked)

---

## 🚨 CRITICAL — Sitemap lists 300+ nonexistent pages

`sitemap.xml` has **345 entries**, but only ~11 HTML files exist in the repo. Approximately **334 URLs in the sitemap return 404** when Google crawls them. This includes entire planned content clusters:
- 82+ `ai-visibility-for-*.html` pages (dentists, plumbers, HVAC, roofing, etc.)
- 100+ `[industry]-marketing-[city].html` pages
- `get-recommended-by-chatgpt.html`, `what-is-geo.html`
- `industries.html`, `guides.html`, `locations.html`
- Blog/article pages (`how-to-rank-on-perplexity.html`, `is-seo-dead.html`, etc.)

**Impact:** Google Webmaster submits these as canonical URLs. Crawling 334 dead links wastes crawl budget and sends negative signals. A sitemap should only list pages that exist and return 200.

**Fix:** Remove all nonexistent URLs from `sitemap.xml` until the pages are actually built and deployed. Or, if these are planned, set them behind a `noindex` redirect holding page until content is live.

---

## ⚠️ HIGH — Broken internal navigation links (user-facing 404s)

The following links appear in **in-page navigation and FAQ sections** (not just sitemap) and will return 404 to users:

| Missing file | Referenced in |
|---|---|
| `get-recommended-by-chatgpt.html` | index.html (FAQ), ai-automation-toronto.html, ai-automation-mississauga.html, ai-automation-gta.html, ai-receptionist.html |
| `what-is-geo.html` | ai-automation-toronto.html, ai-automation-mississauga.html, ai-automation-gta.html, ai-receptionist.html |

Users clicking these links hit a 404. These are also in linked FAQ text in structured data.

**Fix:** Either create these two pages or remove the links until the pages exist.

---

## ⚠️ MEDIUM — `privacy.html` missing SEO meta

`privacy.html` has a `<title>` and `<h1>` but is missing:
- `<meta name="description">`
- `<link rel="canonical">`
- `<meta property="og:*">` / `<meta name="twitter:*">`
- No `rel="apple-touch-icon"` link

It also appears in the sitemap. A canonical tag at minimum would prevent any duplicate-content signals from query-string variants.

---

## ⚠️ MEDIUM — `clinic-chatbot.html` is in sitemap with no SEO meta

`clinic-chatbot.html` is a client-facing demo widget (Northview Psychology chatbot) with no `<meta name="description">`, no canonical, no og/twitter tags, and no `<h1>`. It's linked from `case-clinic-receptionist.html` and is indexed in the sitemap.

Google will crawl it as a standalone page. It looks thin to crawlers and has no indexing controls. Recommend either:
1. Adding `<meta name="robots" content="noindex">` (easiest, it's a demo widget)
2. Or adding full SEO meta if you want it indexed.

---

## ✅ CLEAN — Asset references

All local assets referenced in HTML exist in the repo:
- `assets/og-image.png` ✅
- `assets/favicon.png` ✅
- `assets/apple-touch-icon.png` ✅
- `assets/favicon-192.png` ✅ (used in schema JSON-LD)
- `ds/trivox-icon.png` ✅
- `ds/sign.jpg` ✅
- `ds/tokens.css`, `ds/motion.css`, `ds/site.css`, `ds/v2.css`, `ds/motion.js`, `ds/viz.js`, `ds/scroll.js` ✅

No broken images or missing stylesheets.

---

## ✅ CLEAN — SEO / social meta (core marketing pages)

All 9 core marketing pages (index through workflow-automation) have:
- `<title>` present and non-empty ✅
- `<meta name="description">` ✅
- `<link rel="canonical">` ✅
- `og:title`, `og:description`, `og:image`, `og:url` ✅
- `twitter:card`, `twitter:image` ✅
- `og:image` file (`assets/og-image.png`) exists in repo ✅
- `<meta name="viewport">` ✅
- Favicon link ✅
- `<html lang="en">` ✅

---

## ✅ CLEAN — Accessibility

- Every `<img>` tag has a non-empty `alt` attribute ✅
- All pages have `<html lang="en">` ✅
- Each marketing page has exactly one `<h1>` ✅
- `clinic-chatbot.html` has 0 `<h1>` but is a demo widget, not a content page

---

## ✅ CLEAN — Hidden-content / mobile reveal bugs

`[data-reveal]` pattern uses `IntersectionObserver` with **threshold: 0.15** and `rootMargin: '0px 0px -8% 0px'`. This is well within safe range for mobile — only 15% of element visibility required. No tall section + high-threshold combination found.

Reduced-motion fallback correctly uses `!important` to force reveal without animation. GSAP animations operate on transform only (not opacity), so opacity is never JS-gated.

---

## ✅ CLEAN — Forms

No `<form>` elements found on any marketing page. All CTAs link externally to cal.com, Stripe, or mailto. No form validation or method/action issues.

---

## ✅ CLEAN — Internal anchors

All in-page anchor targets (`#services`, `#work`, `#store`, `#about`, `#faq`) verified to have matching `id` attributes on index.html. All tel:/mailto: URIs well-formed.

---

## Cognizance

Cognizance: source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).

---

*Generated by TRIVOX QA Agent · 2026-08-30*
