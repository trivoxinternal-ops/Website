# TRIVOX QA Report — 2026-07-26

**Overall status:** ⚠️ 4 issues (0 critical, 1 moderate, 3 low)  
**Pages audited:** 8 (index.html, ai-automation-toronto.html, ai-voice-agents.html, workflow-automation.html, case-car-deals-direct.html, case-clinic-receptionist.html, clinic-chatbot.html, privacy.html)  
**Liveness:** Not checked (remote egress blocked by proxy — not a site error)  
**Cognizance:** Source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it)

---

## 1. Broken / Missing Asset References — ✅ CLEAN

All local asset references verified against the repo filesystem:

- `ds/trivox-icon.png`, `ds/sign.jpg`, `ds/v2.css`, `ds/scroll.js`, `ds/motion.js`, `ds/motion.css`, `ds/site.css`, `ds/tokens.css`, `ds/viz.js` — all present
- `assets/favicon.png`, `assets/apple-touch-icon.png`, `assets/og-image.png` — all present
- OG image (`assets/og-image.png`) confirmed in repo — all 8 pages reference it correctly

No broken local asset references found.

---

## 2. SEO / Social Meta

### Main pages (index, ai-automation-toronto, ai-voice-agents, workflow-automation, case-car-deals-direct, case-clinic-receptionist) — ✅ CLEAN

All 6 main pages have:
- Non-empty `<title>` ✅
- `<meta name="description">` ✅
- `<link rel="canonical">` ✅
- `og:title`, `og:description`, `og:image`, `og:url` ✅
- `twitter:card`, `twitter:image` ✅
- Viewport meta ✅
- Favicon + apple-touch-icon ✅
- OG image file verified in `assets/` ✅

### ⚠️ clinic-chatbot.html — MISSING SEO META (MODERATE)

This page has no `<meta name="description">`, no canonical, no og:* tags, no twitter:* tags, and no `<meta name="robots">` directive. It will be crawled and indexed by Google with no metadata — appearing as a blank or confusing result.

**Fix options:**
- If it's an embedded widget: add `<meta name="robots" content="noindex, nofollow">`.
- If it's a discoverable demo: add full SEO meta (title is "Northview Psychology — AI Receptionist" — unclear if this is meant to rank).

### ⚠️ privacy.html — MISSING CANONICAL + DESCRIPTION (LOW)

No `<meta name="description">`, no `<link rel="canonical">`, no og:* or twitter:* tags.

**Fix:** Add at minimum a canonical (`https://trivox.ca/privacy.html`) and `<meta name="robots" content="noindex">` (privacy pages generally should not rank).

---

## 3. Accessibility — 2 low issues

### ✅ CLEAN
- All pages have `<html lang="en">`
- All pages have exactly one `<h1>` (except clinic-chatbot — see below)
- All `<img>` tags have descriptive `alt` text

### ⚠️ Heading level skip h2 → h4 (LOW, multiple pages)

The following sections skip h3 entirely, jumping from h2 to h4:
- `index.html` `#about` → `.vals` uses `<h4>` (Practical AI, Automation first, Human-level)
- `ai-automation-toronto.html`, `ai-voice-agents.html`, `workflow-automation.html` → `.out .o` outcome cards use `<h4>` under a `<h2>` head

This breaks heading hierarchy for screen readers.  
**Fix:** Change `<h4>` to `<h3>` in the vals/outcomes card sections.

### ⚠️ clinic-chatbot.html — No `<h1>` (LOW)

The chat UI page has no heading elements at all.

---

## 4. Hidden-Content / Mobile Bugs — ✅ CLEAN

Reveal system analysis:

- `motion.css`: `[data-reveal]` starts hidden (`opacity:0; transform:translateY(26px)`)
- `motion.js`: IntersectionObserver with `threshold: 0.15` and `rootMargin: '0px 0px -8% 0px'`
- **Threshold 0.15 is safe on mobile** — elements reveal when just 15% visible. No risk of elements getting stuck invisible on narrow/short viewports.
- `prefers-reduced-motion` is correctly respected: all reveals are force-applied immediately.
- GSAP scroll animations on `index.html` explicitly animate **transform only** (not opacity), so scroll-trigger misfires cannot create invisible sections. Comment in code confirms this is intentional.
- No high-threshold (≥0.4) IntersectionObserver patterns found anywhere.

---

## 5. Forms — ✅ CLEAN

No `<form>` elements on any page. All CTAs use external links (Calendly, Stripe, mailto:). No form validation issues.

---

## 6. Internal Links

### ✅ CLEAN — All in-page anchors resolve

Checked `index.html` nav anchors:
- `#services` → `id="services"` ✅
- `#work` → `id="work"` ✅
- `#store` → `id="store"` ✅
- `#about` → `id="about"` ✅
- `#faq` → `id="faq"` ✅

All cross-page `.html` links point to files that exist. All `mailto:` and `tel:` links are well-formed.

### ⚠️ `<a href="#">` on hours text — 4 pages (LOW)

On `index.html`, `ai-automation-toronto.html`, `ai-voice-agents.html`, and `workflow-automation.html`, the "Mon–Fri, 9–6 EST" text is wrapped in `<a href="#">`. Clicking it scrolls back to the top of the page — confusing UX.

**Fix:** Replace `<a href="#">` with `<span>` for non-link text.

---

## Issue Summary

| Severity | Page | Issue |
|---|---|---|
| Moderate | clinic-chatbot.html | No SEO meta, no og:*, no canonical, no robots directive |
| Low | privacy.html | Missing canonical, description, og:* |
| Low | index.html + 3 others | h2→h4 heading skip in card sections |
| Low | index.html + 3 others | `<a href="#">` on hours text (scrolls to top) |

---

*Generated by TRIVOX QA Agent · 2026-07-26*
