# TRIVOX QA Report — 2026-09-04

**Pages audited:** index.html, ai-automation-toronto.html, ai-automation-gta.html, ai-automation-mississauga.html, ai-voice-agents.html, ai-receptionist.html, workflow-automation.html, case-clinic-receptionist.html, case-car-deals-direct.html, clinic-chatbot.html, privacy.html (11 pages)  
**Overall status:** ⚠️ 2 real issues found

---

## ❌ Issue 1 — BROKEN LINKS (HIGH): Two missing pages linked sitewide

Both `what-is-geo.html` and `get-recommended-by-chatgpt.html` are referenced in the HTML but **do not exist in the repository**. Clicking them will 404.

**Affected pages (5 each):**
- `index.html` (line 335) — inside FAQ answer
- `ai-automation-toronto.html` (line 244/245) — "Related" section rows
- `ai-automation-gta.html` (lines 263/264) — "Related" section rows
- `ai-automation-mississauga.html` (lines 232/233) — "Related" section rows
- `ai-receptionist.html` (line 218) — inline prose links

**Action:** Create these two pages (or point the links to an existing page or remove them).

---

## ⚠️ Issue 2 — clinic-chatbot.html: No SEO metadata + no H1

`clinic-chatbot.html` is a publicly accessible page titled "Northview Psychology — AI Receptionist" (a client demo). It is missing:
- `<meta name="description">` — absent
- `<link rel="canonical">` — absent
- `<h1>` — absent (only H2+ are used)
- `<meta property="og:…">` / `<meta name="twitter:…">` — absent

If crawled, this will surface a competitor's brand name under trivox.ca and produce thin-content signals.

**Action:** Either add `<meta name="robots" content="noindex">` to suppress crawling, or add proper TRIVOX-branded metadata and a real H1.

---

## ✅ Broken / Missing Assets
All local assets verified present:
- `ds/trivox-icon.png`, `ds/sign.jpg`, `ds/trivox-mark.png` ✓
- `ds/tokens.css`, `ds/motion.css`, `ds/site.css`, `ds/v2.css`, `ds/scroll.js`, `ds/motion.js`, `ds/viz.js` ✓
- `assets/favicon.png`, `assets/apple-touch-icon.png`, `assets/og-image.png`, `assets/favicon-192.png` ✓

No broken local image or CSS references found.

---

## ✅ SEO / Social Meta
All 10 main pages (excluding clinic-chatbot.html) have:
- `<title>` — non-empty ✓
- `<meta name="description">` — present ✓
- `<link rel="canonical">` — present ✓
- `og:title`, `og:description`, `og:image`, `og:url` — present ✓
- `twitter:card`, `twitter:image` — present ✓
- `<meta name="viewport">` — present ✓
- Favicon links — present ✓

`og:image` on all pages points to `https://trivox.ca/assets/og-image.png` — file exists in repo ✓

---

## ✅ Accessibility
- `<html lang="en">` — present on all 11 pages ✓
- All `<img>` tags have non-empty `alt` attributes ✓
- Exactly 1 `<h1>` on all pages **except** `clinic-chatbot.html` (0 H1s) — see Issue 2
- No obviously skipped heading levels on main pages

---

## ✅ Hidden-Content / Mobile Bugs
- `[data-reveal]` elements use `opacity:0` + `translateY(26px)` via `motion.css`
- IntersectionObserver threshold: **0.15** (15%) + rootMargin `0px 0px -8% 0px` — **safe for mobile**
- GSAP ScrollTrigger animations (scroll.js) use `start: 'top 78%'–'top 84%'` — safe
- `prefers-reduced-motion` is respected: all reveals immediately set to `.in` on reduced-motion ✓
- No element found with a high (≥0.4) threshold risk

---

## ✅ Forms
Only 1 form-like input found in clinic-chatbot.html (chat widget). It has no `name` attribute, but this is a JS-driven chat UI (not a POST form) — no action/submit, so this is intentional. Clean.

---

## ✅ Internal Links
All in-page anchor links on index.html verified:
- `#services` → `id="services"` ✓
- `#work` → `id="work"` ✓
- `#store` → `id="store"` ✓
- `#about` → `id="about"` ✓
- `#faq` → `id="faq"` ✓

`mailto:info@trivox.ca` — well-formed ✓  
`tel:` links — none present (phone number in Schema only)

---

## Liveness
Liveness: not checked (remote egress blocked).

---

## Cognizance
Cognizance: source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).
