# TRIVOX QA — Daily Audit 2026-09-12

**Auditor:** TRIVOX AI QA Agent (automated)  
**Repo:** trivoxinternal-ops/Website (main)  
**Pages audited:** 11 HTML pages  
**Overall status:** ⚠️ 5 issues (0 critical, 2 medium, 3 low)

> **Note on last report:** QA-2026-09-10 incorrectly marked "Internal Links — ✅ CLEAN". The two missing linked pages (a recurring issue since 2026-09-03) were not re-checked. This report re-flags them.

---

## Pages Audited

- `index.html`
- `ai-automation-toronto.html`
- `ai-automation-gta.html`
- `ai-automation-mississauga.html`
- `ai-receptionist.html`
- `ai-voice-agents.html`
- `workflow-automation.html`
- `case-car-deals-direct.html`
- `case-clinic-receptionist.html`
- `clinic-chatbot.html`
- `privacy.html`

---

## 1. Broken / Missing Asset References — ✅ CLEAN

All local asset references (`assets/`, `ds/`) exist in the repo. No broken images, stylesheets, or scripts.

- `assets/favicon.png`, `assets/apple-touch-icon.png`, `assets/og-image.png` ✅
- `ds/site.css`, `ds/v2.css`, `ds/tokens.css`, `ds/motion.css` ✅
- `ds/motion.js`, `ds/scroll.js`, `ds/viz.js` ✅
- `ds/sign.jpg`, `ds/trivox-icon.png` ✅

All `og:image` references point to `https://trivox.ca/assets/og-image.png` — file exists ✅

---

## 2. SEO / Social Meta — ⚠️ 2 issues (carried over)

### clinic-chatbot.html — MEDIUM ⚠️ (Day 10+ unresolved)
Missing: `<meta name="description">`, `<link rel="canonical">`, `og:title`, `og:description`, `og:image`, `twitter:card`, `twitter:image`.  
Page is publicly accessible at `/clinic-chatbot.html`. If intentionally non-indexed, add `<meta name="robots" content="noindex">`.

### privacy.html — LOW ⚠️ (carried over)
Missing: `<link rel="canonical">`, og/twitter meta. Low priority for a legal page, but canonical is a quick win.

### All other pages — ✅ CLEAN
All 9 main pages have: `<title>`, `meta description`, `canonical`, `og:title/description/image`, `twitter:card/image`, `viewport`, `lang="en"`, favicon `<link rel="icon">`. OG image file exists in repo. ✅

---

## 3. Accessibility — ⚠️ 2 issues (carried over)

### clinic-chatbot.html — No `<h1>` — MEDIUM ⚠️
Zero in-page `<h1>` headings. Add a visually-hidden `<h1>` or mark page `noindex`.

### h2 → h4 heading skip — LOW ℹ️
Seven pages jump from `<h2>` to `<h4>` in feature-triptych sections (e.g., "Practical AI / Automation first / Human-level"), skipping `<h3>`. Affected: `index.html`, `ai-automation-gta.html`, `ai-automation-mississauga.html`, `ai-automation-toronto.html`, `ai-receptionist.html`, `ai-voice-agents.html`, `workflow-automation.html`. Consider replacing `<h4>` with `<p class="feature-label">` or `<strong>` — these are visual labels, not structural headings.

### Everything else — ✅ CLEAN
- All `<html>` elements have `lang="en"` ✅
- All pages (except `clinic-chatbot.html`) have exactly one `<h1>` ✅
- No `<img>` tags missing `alt` attributes ✅

---

## 4. Hidden-content / Mobile Bugs — ✅ CLEAN

- `[data-reveal]` IntersectionObserver uses `threshold: 0.15` — low, safe for all screen sizes ✅
- `prefers-reduced-motion` fallback: `opacity:1 !important; transform:none !important` — elements never stuck hidden ✅
- GSAP `scroll.js` animates **transform only** (opacity handled by `motion.js [data-reveal]`), so GSAP failures cannot create invisible sections ✅
- No high-threshold patterns found ✅

No mobile black-gap risks.

---

## 5. Forms — ✅ CLEAN

No `<form>` elements on any page. All CTAs use external booking/payment links. No form issues.

---

## 6. Internal Links — ⚠️ MEDIUM (persistent, Day 10+)

### Two missing pages — 404 on live site

The following pages are **linked but do not exist** in the repo and have never existed:

| File | Linked from |
|------|-------------|
| `get-recommended-by-chatgpt.html` | `index.html` FAQ anchor text + ledger row; `ai-automation-toronto.html` ledger |
| `what-is-geo.html` | `index.html` FAQ anchor text + ledger row; `ai-automation-toronto.html` ledger |

These are live 404 errors for any visitor who clicks those links. Both pages are likely **planned content** (GEO/AEO guides). Until the pages are built, either:
- Remove/replace the links temporarily, or
- Create stub pages with a redirect or "coming soon" message.

This issue has been flagged in QA reports since at least **2026-09-03** (10+ days).

### Inconsistent booking URL — case-car-deals-direct.html — LOW ⚠️
All 3 CTAs on `case-car-deals-direct.html` use `https://calendly.com/trivoxinternal/intro-call` — the old booking URL. Every other page uses `https://cal.com/trivox-interno-hfbm3x/trivox-discovery-call`. This is a stale link. Update to match the rest of the site.

### Named section anchors — ✅ CLEAN
All `index.html` in-page anchors (`#about`, `#faq`, `#services`, `#store`, `#work`) have matching `id=` targets ✅.  
Tel/mailto: `mailto:info@trivox.ca` is well-formed ✅.

---

## 7. Liveness

Liveness: not checked (remote egress blocked — WebFetch/curl unavailable in this environment).

---

## Cognizance

Cognizance: source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).

---

## Summary Action Items

| Priority | Page(s) | Issue | Fix |
|----------|---------|-------|-----|
| **Medium** | `index.html`, `ai-automation-toronto.html` | Links to `get-recommended-by-chatgpt.html` and `what-is-geo.html` — 404 on live site (10+ days) | Build pages OR remove/stub the links |
| **Medium** | `clinic-chatbot.html` | No SEO meta, no canonical, no `<h1>` | Add `noindex` + hidden h1, or add full meta |
| **Low** | `case-car-deals-direct.html` | Old Calendly booking URL on all CTAs | Replace with `cal.com/trivox-interno-hfbm3x/trivox-discovery-call` |
| **Low** | `privacy.html` | No canonical | Add `<link rel="canonical" href="https://trivox.ca/privacy.html">` |
| **Low** | 7 pages | h2 → h4 heading skip | Change `<h4>` feature labels to `<p>` or `<strong>` |

---

*Report generated by TRIVOX AI QA Agent — 2026-09-12*
