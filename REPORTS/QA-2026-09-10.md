# TRIVOX QA — Daily Audit 2026-09-10

**Auditor:** TRIVOX AI QA Agent (automated)  
**Repo:** trivoxinternal-ops/Website (main)  
**Pages audited:** 11 HTML pages  
**Overall status:** ⚠️ 3 issues found (0 critical, 2 medium, 1 low)

---

## Pages Audited
- `index.html`
- `ai-automation-toronto.html`
- `ai-automation-gta.html`
- `ai-automation-mississauga.html`
- `ai-receptionist.html`
- `ai-voice-agents.html`
- `workflow-automation.html`
- `case-car-deals-direct.html`
- `case-clinic-receptionist.html`
- `clinic-chatbot.html`
- `privacy.html`

---

## 1. Broken / Missing Asset References — ✅ CLEAN

All local asset references (`assets/`, `ds/`) exist in the repo. No broken images, stylesheets, or scripts detected.

- `assets/favicon.png` ✅
- `assets/apple-touch-icon.png` ✅
- `assets/og-image.png` ✅
- `ds/site.css`, `ds/v2.css`, `ds/tokens.css`, `ds/motion.css` ✅
- `ds/motion.js`, `ds/scroll.js`, `ds/viz.js` ✅
- `ds/sign.jpg`, `ds/trivox-icon.png` ✅

External GSAP CDN (`cdn.jsdelivr.net`) is used by pages with scroll.js — not verified (CDN, expected working).

---

## 2. SEO / Social Meta — ⚠️ 2 issues

### clinic-chatbot.html — MEDIUM ⚠️
Missing: `<meta name="description">`, `og:title`, `og:description`, `og:image`, `twitter:card`, `twitter:image`, `<link rel="canonical">`.

This page (Northview Psychology AI Receptionist demo) is publicly accessible. If it's intentionally a non-indexed widget page, add `<meta name="robots" content="noindex">` to make that explicit. Otherwise, add full meta.

### privacy.html — LOW ⚠️
Missing: `<link rel="canonical">`, og/twitter social meta.

Privacy pages rarely need social sharing cards, but a canonical is recommended. Low priority.

### All main/product pages — ✅ CLEAN
`index.html`, all `ai-*.html`, `workflow-automation.html`, `case-*.html`: all have `<title>`, `meta description`, `canonical`, `og:title`, `og:description`, `og:image` (pointing to `https://trivox.ca/assets/og-image.png` — file exists ✅), `twitter:card`, `twitter:image`, `viewport`, and favicon `<link rel="icon">`.

---

## 3. Accessibility — ⚠️ 1 issue

### clinic-chatbot.html — No `<h1>` — MEDIUM ⚠️
The page has zero `<h1>` headings. The page title is set only in `<title>` and shows "Northview Psychology — AI Receptionist" but there's no in-page `<h1>` for screen readers. If this is a client demo page, add a visually-hidden `<h1>` or add `noindex`.

### Heading level skips — LOW ℹ️
Multiple pages jump from `<h2>` directly to `<h4>` in feature-list sections (the "Practical AI / Automation first / Human-level" triptychs), skipping `<h3>`. Affected pages: `index.html`, `ai-automation-gta.html`, `ai-automation-mississauga.html`, `ai-automation-toronto.html`, `ai-receptionist.html`, `ai-voice-agents.html`, `workflow-automation.html`.

This is a minor accessibility concern but consistent across all pages — the `<h4>` cards are visually styled as feature labels, not section headings, so consider changing them to `<p>` or `<strong>` instead.

### Everything else — ✅ CLEAN
- All `<html>` elements have `lang="en"` ✅
- All pages (except clinic-chatbot.html) have exactly **one** `<h1>` ✅
- No `<img>` tags are missing `alt` attributes ✅
- No `<img>` tags have empty `alt=""` ✅

---

## 4. Hidden-content / Mobile Bugs — ✅ CLEAN

The `[data-reveal]` scroll animation system uses:
- **IntersectionObserver threshold: 0.15** (15%) — safe for mobile; low enough that tall elements will trigger before needing 40%+ in view
- **rootMargin: `0px 0px -8% 0px`** — reasonable negative bottom margin
- **Prefers-reduced-motion fallback:** `motion.css` sets `opacity:1 !important` and `transform:none !important` for `[data-reveal]` under `prefers-reduced-motion: reduce` ✅
- **GSAP scroll.js:** animates transform only (no opacity), so if GSAP fails to load, elements are still visible — no invisible stuck elements risk ✅
- **No high-threshold IntersectionObserver patterns detected** ✅

No mobile black-gap risks found.

---

## 5. Forms — ✅ CLEAN (No forms present)

No `<form>` elements found on any page. CTAs use `mailto:` links and external booking integrations. No form issues to flag.

---

## 6. Internal Links — ✅ CLEAN

Named section anchors on `index.html` all resolve correctly:
- `#about` ✅, `#faq` ✅, `#services` ✅, `#store` ✅, `#work` ✅

`href="#"` (scroll-to-top) links appear on most pages — these are a standard pattern, not dangling anchors.

Tel/mailto links: properly formed `mailto:info@trivox.ca` — no malformation detected.

Other pages (product/case study pages) have no named in-page anchors.

---

## Liveness
Liveness: not checked (remote egress blocked — curl/WebFetch unavailable in this environment).

---

## Cognizance
Cognizance: source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).

---

## Action Items

| Priority | Page | Issue | Recommended Fix |
|----------|------|-------|-----------------|
| Medium | `clinic-chatbot.html` | No meta description, no og/twitter, no canonical, no `<h1>` | Either add `<meta name="robots" content="noindex">` + visually-hidden `<h1>`, or add full SEO meta |
| Low | `privacy.html` | No canonical URL | Add `<link rel="canonical" href="https://trivox.ca/privacy.html">` |
| Low | 7 pages | h2→h4 heading skip in feature triptychs | Change `<h4>` feature labels to `<p>` or `<strong>` elements |

---

*Report generated by TRIVOX AI QA Agent — 2026-09-10*
