# TRIVOX QA — Daily Audit 2026-09-13

**Auditor:** TRIVOX AI QA Agent (automated)  
**Repo:** trivoxinternal-ops/Website (main, commit `fd19fd1`)  
**Pages audited:** 11 HTML pages  
**Overall status:** ⚠️ 8 issues (3 new vs Sep-12 report, 2 medium, 3 low + 3 new findings) — **no code changes since 2026-09-12**

> **Note:** No site content was changed between Sep 12 and Sep 13. Three new issues were uncovered today by deeper analysis: (1) `workflow-automation.html` also has stale Calendly URLs — not just `case-car-deals-direct.html`; (2) `clinic-chatbot.html` blocks mobile pinch-zoom (WCAG failure); (3) `index.html` has a no-op `href="#"` on contact hours. The two missing GEO pages have now been live 404s for **11+ consecutive days**.

---

## Pages Audited

| Page | Status |
|------|--------|
| `index.html` | ⚠️ links to 2 missing pages |
| `ai-automation-toronto.html` | ⚠️ links to 2 missing pages |
| `ai-automation-gta.html` | ⚠️ links to 2 missing pages |
| `ai-automation-mississauga.html` | ⚠️ links to 2 missing pages |
| `ai-receptionist.html` | ⚠️ links to 2 missing pages |
| `workflow-automation.html` | ✅ clean |
| `case-clinic-receptionist.html` | ✅ clean |
| `case-car-deals-direct.html` | ⚠️ stale booking URL |
| `clinic-chatbot.html` | ⚠️ no SEO meta, no h1 |
| `privacy.html` | ⚠️ no canonical / og meta |
| `ai-voice-agents.html` | ✅ clean |

---

## 1. Broken / Missing Asset References — ✅ CLEAN

All local asset references (`assets/`, `ds/`) verified present in repo:

- `assets/favicon.png`, `assets/apple-touch-icon.png`, `assets/og-image.png` ✅
- `ds/site.css`, `ds/v2.css`, `ds/tokens.css`, `ds/motion.css` ✅
- `ds/motion.js`, `ds/scroll.js`, `ds/viz.js` ✅
- `ds/sign.jpg`, `ds/trivox-icon.png` ✅

All `og:image` references point to `https://trivox.ca/assets/og-image.png` — file exists in repo ✅

---

## 2. SEO / Social Meta — ⚠️ 2 issues

### clinic-chatbot.html — MEDIUM ⚠️ (Day 11+ unresolved)
Missing all SEO and social meta: `<meta name="description">`, `<link rel="canonical">`, `og:title`, `og:description`, `og:image`, `twitter:card`, `twitter:image`.  
This is a publicly accessible URL. **Quick fix:** add `<meta name="robots" content="noindex, nofollow">` and a visually-hidden `<h1>` if it's intended as a demo-only page.

### privacy.html — LOW ⚠️ (persistent)
Missing `<link rel="canonical">` and all og/twitter meta. Low-priority for a legal page, but canonical is a one-liner fix.

### All other 9 pages — ✅ CLEAN
Every main page has: `<title>`, `meta description`, `canonical`, `og:title/description/image`, `twitter:card/image`, `viewport`, `lang="en"`, and favicon link. ✅

---

## 3. Accessibility — ⚠️ 3 issues

### clinic-chatbot.html — No `<h1>` + `user-scalable=no` — MEDIUM ⚠️ (+ NEW finding)
Zero in-page `<h1>` headings. The page's `<title>` reads "Northview Psychology — AI Receptionist" (a client demo name). Add a visually-hidden `<h1>` or mark page `noindex`.

**NEW:** Line 5 — `<meta name="viewport" content="... user-scalable=no">` blocks mobile pinch-to-zoom. This is a **WCAG 1.4.4 (Resize Text, Level AA) failure** — should be removed regardless of whether the page is a demo. Remove `maximum-scale=1.0, user-scalable=no`.

### h2 → h4 heading skip — LOW ℹ️ (cosmetic, low urgency)
Seven pages jump from `<h2>` to `<h4>` in feature-triptych sections ("Practical AI / Automation first / Human-level"), skipping `<h3>`. Affected: `index.html`, `ai-automation-gta.html`, `ai-automation-mississauga.html`, `ai-automation-toronto.html`, `ai-receptionist.html`, `ai-voice-agents.html`, `workflow-automation.html`.  
Consider replacing `<h4>` with `<p class="feature-label">` or `<strong>` — these are visual labels, not structural headings.

### All other checks — ✅ CLEAN
- All `<html>` elements have `lang="en"` ✅
- All pages (except `clinic-chatbot.html`) have exactly one `<h1>` ✅
- No `<img>` tags missing `alt` attributes ✅

---

## 4. Hidden-content / Mobile Bugs — ✅ CLEAN

- `[data-reveal]` IntersectionObserver: `threshold: 0.15`, `rootMargin: '0px 0px -8% 0px'` — low threshold, safe on all screen sizes ✅
- `prefers-reduced-motion` fallback: `opacity:1 !important; transform:none !important` in `motion.css` — elements can never get stuck invisible ✅
- `scroll.js` animates **transform only** (no opacity), so GSAP failures cannot create invisible/black sections ✅
- No high-threshold reveal patterns found ✅

No mobile black-gap risks.

---

## 5. Forms — ✅ CLEAN

No `<form>` elements on any page. All CTAs use external booking/calendar links. No form issues. (`clinic-chatbot.html` has a `<input>` inside a chat UI — no `name` attribute needed, it's JS-driven, not a submitted form.)

---

## 6. Internal Links — ⚠️ MEDIUM (persistent — Day 11+)

### Two missing pages — 404 on live site (🔴 ESCALATING)

The following pages are **linked but do not exist** in the repo. This is an active 404 for any visitor who clicks these links:

| Missing file | Linked from |
|---|---|
| `get-recommended-by-chatgpt.html` | `index.html` (FAQ + ledger), `ai-automation-toronto.html`, `ai-automation-gta.html`, `ai-automation-mississauga.html`, `ai-receptionist.html` — **5 pages** |
| `what-is-geo.html` | `index.html` (FAQ + ledger), `ai-automation-toronto.html`, `ai-automation-gta.html`, `ai-automation-mississauga.html`, `ai-receptionist.html` — **5 pages** |

**First flagged: 2026-09-02 (11 days ago).** No fix has shipped.  
Recommended: build the pages, or temporarily remove the links until content is ready.

### case-car-deals-direct.html + workflow-automation.html — Stale Calendly URLs — LOW ⚠️ (NEW: scope wider than Sep-12 report)
**Both pages** have stale `https://calendly.com/trivoxinternal/intro-call` links instead of the current `cal.com` URL:
- `case-car-deals-direct.html`: 3 CTAs + JSON-LD `offers.url` (4 occurrences)
- `workflow-automation.html`: nav CTA + 2 hero CTAs + 4 ledger rows + JSON-LD `offers.url` (8 occurrences)

All other pages use `https://cal.com/trivox-interno-hfbm3x/trivox-discovery-call`. A find-and-replace in each file fixes this.

### index.html — `href="#"` no-op link — LOW ⚠️ (NEW)
Line 480: contact hours text ("Mon–Fri, 9–6 EST") is wrapped in `<a href="#">` — clicking it silently scrolls to the top of the page. Remove the anchor tag and leave it as plain text, or point it to a real anchor.

### Named section anchors — ✅ CLEAN
All `index.html` in-page anchors (`#about`, `#faq`, `#services`, `#store`, `#work`) have matching `id=` targets ✅.  
`mailto:info@trivox.ca` and `mailto:daniel@trivox.ca` are well-formed ✅.

---

## 7. Liveness

Liveness: not checked (remote egress blocked — WebFetch unavailable in this environment).

---

## Cognizance

Cognizance: source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).

---

## Summary Action Items

| Priority | Day # | Page(s) | Issue | Fix |
|----------|-------|---------|-------|-----|
| **Medium** | Day 11 | 5 pages | Links to `get-recommended-by-chatgpt.html` and `what-is-geo.html` — live 404s | Build pages OR remove/stub links immediately |
| **Medium** | Day 11 | `clinic-chatbot.html` | No SEO meta, no `<h1>`, `user-scalable=no` (WCAG fail) | Add `noindex` + hidden `<h1>`; remove `user-scalable=no` |
| **Low** | NEW | `case-car-deals-direct.html` + `workflow-automation.html` | Old Calendly URL (12 total occurrences, incl. JSON-LD) | Find-and-replace with `cal.com/trivox-interno-hfbm3x/trivox-discovery-call` |
| **Low** | NEW | `index.html` | `href="#"` on contact hours — scrolls to top on click | Remove `<a>` wrapper or point to a real target |
| **Low** | Day 11 | `privacy.html` | No canonical or og/twitter meta | Add `<link rel="canonical" href="https://trivox.ca/privacy.html">` |
| **Low** | Day 11 | 8 pages | `h2 → h4` heading skip in feature triptychs | Change `<h4>` to `<p>` or `<strong>` |

---

*Report generated by TRIVOX AI QA Agent — 2026-09-13*
