# TRIVOX QA Report — 2026-09-03

**Audited by:** TRIVOX AI QA Agent (automated, daily)
**Pages audited:** 11 HTML pages (index.html, ai-automation-gta.html, ai-automation-mississauga.html, ai-automation-toronto.html, ai-receptionist.html, ai-voice-agents.html, case-car-deals-direct.html, case-clinic-receptionist.html, clinic-chatbot.html, privacy.html, workflow-automation.html)
**Repo:** trivoxinternal-ops/Website (branch: main, latest pull: ce23f69..abf66e5)
**Overall status:** ⚠️ 2 pages with moderate SEO gaps (no critical/broken issues)

---

## 1. Broken / Missing Asset References

**✅ CLEAN**

All local asset references verified against repo files:

| Asset | Status |
|---|---|
| `assets/apple-touch-icon.png` | ✅ exists |
| `assets/favicon.png` | ✅ exists |
| `ds/motion.css` | ✅ exists |
| `ds/motion.js` | ✅ exists |
| `ds/scroll.js` | ✅ exists |
| `ds/sign.jpg` | ✅ exists |
| `ds/site.css` | ✅ exists |
| `ds/tokens.css` | ✅ exists |
| `ds/trivox-icon.png` | ✅ exists |
| `ds/v2.css` | ✅ exists |
| `ds/viz.js` | ✅ exists |

OG/Twitter images all point to `https://trivox.ca/assets/og-image.png` (absolute URL). File `assets/og-image.png` exists in repo. ✅

---

## 2. SEO / Social Meta

**⚠️ 2 pages with gaps:**

### clinic-chatbot.html — MISSING ALL SEO META
This appears to be a client-facing live demo (Northview Psychology). It is missing:
- `<meta name="description">` — ❌ absent
- `<link rel="canonical">` — ❌ absent
- `<meta property="og:title">` — ❌ absent
- `<meta property="og:description">` — ❌ absent
- `<meta property="og:image">` — ❌ absent
- `<meta name="twitter:card">` — ❌ absent
- `<meta name="twitter:image">` — ❌ absent
- `<h1>` — ❌ absent (see Accessibility)

**Action:** Add `<meta name="robots" content="noindex">` if this is a client demo not meant to be indexed, OR add full SEO meta if it should be findable.

### privacy.html — MISSING SEO META
- `<meta name="description">` — ❌ absent
- `<link rel="canonical">` — ❌ absent
- `<meta property="og:*">` — ❌ absent (no og block at all)
- `<meta name="twitter:*">` — ❌ absent

**Action:** Add at minimum a description and canonical. OG/Twitter tags optional for a policy page (but canonical is important to avoid duplicate-content signals).

### All other pages — ✅ CLEAN
All 9 main pages (index, 4 service pages, 3 geo pages, 1 workflow page) have complete SEO meta: title, description, canonical, og:title, og:description, og:image, twitter:card, twitter:image, viewport, favicon/apple-touch-icon.

---

## 3. Accessibility

**⚠️ 1 issue:**

### clinic-chatbot.html — No `<h1>`
All other pages: exactly 1 `<h1>` each. ✅
clinic-chatbot.html: 0 `<h1>` tags. — ❌

**Action:** Add a visible or screen-reader-only `<h1>` (e.g. the clinic name or page purpose).

### Everything else — ✅ CLEAN
- All pages have `<html lang="en">` ✅
- No `<img>` tags found with missing or empty `alt` attributes ✅
- Heading hierarchy looks clean across all main pages ✅

---

## 4. Hidden-Content / Mobile Bugs

**✅ CLEAN — no mobile reveal risk**

- `[data-reveal]` pattern uses `opacity:0` + `transform` in `ds/motion.css`
- `ds/motion.js` IntersectionObserver threshold: **0.15** (safe — elements trigger at 15% visibility, works fine on mobile)
- `rootMargin: '0px 0px -8% 0px'` — mild negative bottom margin, not problematic
- `ds/motion.css` line 45 has `@media` prefers-reduced-motion fallback that forces `opacity:1 !important; transform:none !important` — so elements will always be visible for reduced-motion users ✅
- `ds/scroll.js` explicitly notes: *"reveals animate TRANSFORM ONLY (no opacity). Opacity fade stays with motion.js, so nothing can ever get stuck invisible if a ScrollTrigger misfires"* ✅

No high-threshold reveal risks. No element will stay invisible on mobile.

---

## 5. Forms

**⚠️ 1 minor note (non-blocking):**

- `clinic-chatbot.html:397` — `<input type="text" class="chat-input" id="chatInput">` has no `name` attribute.
  - This is a JS-driven chat widget (not a traditional form submission). The missing `name` won't break the widget, but it is non-standard.
- No traditional `<form method= action=>` or `data-netlify` forms found on any page.
- No inputs/textareas missing `name` on actual lead-gen or contact forms.

---

## 6. Internal Links / Anchors

**✅ CLEAN**

- `index.html` links to `#faq` → `<section id="faq">` ✅
- `index.html` links to `#store` → `<section id="store">` ✅
- No dangling in-page anchors found across other pages.
- All `mailto:` links well-formed (`info@trivox.ca`, `daniel@trivox.ca`) ✅
- No `tel:` links found (none used, none broken) ✅

---

## Liveness

Liveness: not checked (remote egress blocked — source audit only).

---

## Cognizance

Cognizance: source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).

---

## Summary Table

| Category | Status | Issues |
|---|---|---|
| Broken assets | ✅ Clean | 0 |
| SEO meta | ⚠️ | 2 pages missing meta (clinic-chatbot, privacy) |
| Accessibility | ⚠️ | 1 page missing h1 (clinic-chatbot) |
| Mobile/hidden content | ✅ Clean | 0 |
| Forms | ✅ Clean | 1 note (chat widget, non-blocking) |
| Internal anchors | ✅ Clean | 0 |

**Priority actions:**
1. **clinic-chatbot.html**: Add `<meta name="robots" content="noindex">` if it's a private demo OR add full SEO meta + h1 if it should rank.
2. **privacy.html**: Add `<meta name="description">` + `<link rel="canonical" href="https://trivox.ca/privacy">`.
