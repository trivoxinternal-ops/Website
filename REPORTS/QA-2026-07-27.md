# TRIVOX QA Report — 2026-07-27

**Overall status:** ⚠️ 4 issues (0 critical, 1 moderate, 3 low)  
**Pages audited:** 8 (index.html, ai-automation-toronto.html, ai-voice-agents.html, workflow-automation.html, case-car-deals-direct.html, case-clinic-receptionist.html, clinic-chatbot.html, privacy.html)  
**New code since yesterday:** None — latest commit is yesterday's QA report (97746ad). All issues below are carry-overs; nothing fixed, nothing new.  
**Liveness:** Not checked (remote egress blocked by proxy — not a site error)  
**Cognizance:** Source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it)

---

## 1. Broken / Missing Asset References — ✅ CLEAN

All local asset references verified against repo filesystem:

- `ds/trivox-icon.png`, `ds/sign.jpg`, `ds/v2.css`, `ds/scroll.js`, `ds/motion.js`, `ds/motion.css`, `ds/site.css`, `ds/tokens.css`, `ds/viz.js` — all present
- `assets/favicon.png`, `assets/apple-touch-icon.png`, `assets/og-image.png`, `assets/favicon-192.png` — all present
- OG image (`assets/og-image.png`) confirmed in repo — all pages reference it correctly

No broken local asset references found.

---

## 2. SEO / Social Meta

### Main pages (index, ai-automation-toronto, ai-voice-agents, workflow-automation, case-car-deals-direct, case-clinic-receptionist) — ✅ CLEAN

All 6 main pages have:
- Non-empty `<title>` ✅
- `<meta name="description">` ✅
- `<link rel="canonical">` ✅
- `og:title`, `og:description`, `og:image`, `og:url` ✅
- `twitter:card`, `twitter:image` ✅
- Viewport meta ✅
- Favicon + apple-touch-icon ✅
- OG image file verified in `assets/` ✅

### ⚠️ clinic-chatbot.html — MISSING SEO META (MODERATE — recurring day 9)

No `<meta name="description">`, no canonical, no og:* tags, no twitter:* tags, no `<meta name="robots">` directive. Google will crawl and index it with no metadata — title shows as "Northview Psychology — AI Receptionist" which could confuse TRIVOX visitors.

**Fix (2 min):** Add `<meta name="robots" content="noindex, nofollow">` inside `<head>` to prevent indexing, or add full TRIVOX-branded meta if it's a landing page.

### ⚠️ privacy.html — MISSING CANONICAL + DESCRIPTION (LOW — recurring)

No `<meta name="description">`, no `<link rel="canonical">`, no og:* or twitter:* tags.

**Fix:** Add `<link rel="canonical" href="https://trivox.ca/privacy.html">` and `<meta name="robots" content="noindex">`.

---

## 3. Accessibility — 2 low issues (recurring)

### ✅ Clean items
- All pages have `<html lang="en">` ✅
- All pages have exactly one `<h1>` (except clinic-chatbot — demo widget, no heading expected) ✅
- All `<img>` tags have descriptive `alt` text ✅
- Viz divs use `role="img"` with `aria-label` ✅

### ⚠️ Heading level skip h2 → h4 (LOW — recurring, 4 pages)

Sections jumping from `<h2>` directly to `<h4>` (skipping h3):
- `index.html` — `#about` `.vals` cards (`<h4>Practical AI</h4>` etc.)
- `ai-automation-toronto.html`, `ai-voice-agents.html`, `workflow-automation.html` — `.out .o` outcome cards

**Fix:** Change `<h4>` to `<h3>` in these card sections.

---

## 4. Hidden-Content / Mobile Bugs — ✅ CLEAN

- `[data-reveal]` uses IntersectionObserver `threshold: 0.15` — safe on mobile (no risk of stuck-invisible elements)
- `rootMargin: '0px 0px -8% 0px'` — triggers early, further reducing mobile gap risk
- GSAP scroll animations on all pages are **transform-only** (not opacity) — confirmed by `scroll.js` comment; a ScrollTrigger misfire cannot leave content invisible
- `prefers-reduced-motion` respected: all reveals force-applied on reduced-motion devices
- `ScrollTrigger.refresh()` called on both `load` event and 700 ms timeout — catches layout-shift stuck triggers
- No IntersectionObserver threshold ≥ 0.4 anywhere

---

## 5. Forms — ✅ CLEAN

No `<form>` elements on any page. All CTAs use external links (Calendly, Stripe, mailto:). Chat widget in `clinic-chatbot.html` handles input via JS event listeners — no traditional form submission concerns.

---

## 6. Internal Links — ✅ CLEAN (1 low cosmetic issue)

All in-page anchors on `index.html` resolve:
- `#services` → `id="services"` ✅
- `#work` → `id="work"` ✅
- `#store` → `id="store"` ✅
- `#about` → `id="about"` ✅
- `#faq` → `id="faq"` ✅

All cross-page `.html` links point to existing files. All `mailto:` links are well-formed.

### ⚠️ `<a href="#">` on hours text — 4 pages (LOW — recurring)

"Mon–Fri, 9–6 EST" is wrapped in `<a href="#">` on `index.html`, `ai-automation-toronto.html`, `ai-voice-agents.html`, and `workflow-automation.html`. Clicking scrolls to top — confusing UX.

**Fix:** Replace `<a href="#">` with `<span>` for this non-link text.

---

## Issue Summary

| Severity | Page(s) | Issue | Days Open |
|---|---|---|---|
| Moderate | clinic-chatbot.html | No SEO/OG/canonical/robots meta — risk of confused Google indexing | 9 |
| Low | privacy.html | Missing canonical, description, robots | 9 |
| Low | index.html + 3 others | h2→h4 heading skip in card sections | 9 |
| Low | index.html + 3 others | `<a href="#">` on hours text | 9 |

**No new issues found today. No fixes applied since yesterday.**

---

*Generated by TRIVOX QA Agent · 2026-07-27*
