# TRIVOX QA — Daily Audit 2026-09-18

**Pages audited:** index.html, ai-automation-toronto.html, ai-voice-agents.html, ai-automation-gta.html, ai-automation-mississauga.html, ai-receptionist.html, case-clinic-receptionist.html, case-car-deals-direct.html, clinic-chatbot.html, workflow-automation.html, privacy.html (11 pages total)

**Overall status: ⚠️ 4 issues (1 critical, 1 moderate, 2 minor)**

---

## 1. Broken / Missing Asset References

**Status: ✅ Clean**

All local asset references verified against repo contents:
- `ds/trivox-icon.png`, `ds/sign.jpg`, `ds/site.css`, `ds/tokens.css`, `ds/motion.css`, `ds/motion.js`, `ds/scroll.js`, `ds/v2.css`, `ds/viz.js` — all present ✓
- `assets/favicon.png`, `assets/apple-touch-icon.png`, `assets/og-image.png` — all present ✓
- External CDN references (Google Fonts, GSAP, Three.js via jsDelivr) — not checked (external)

---

## 2. SEO / Social Meta

**Status: ⚠️ 2 issues**

All 10 indexed pages have `<title>`, `meta description`, `canonical`, `og:title`, `og:description`, `og:image`, `twitter:card`, `twitter:image`, and `viewport` present. `assets/og-image.png` exists in repo. ✓

**Issue 2a (MODERATE) — `clinic-chatbot.html` has zero SEO/social meta:**
The page is publicly accessible at `https://trivox.ca/clinic-chatbot.html` and has no `meta description`, no `og:*` tags, no `canonical`, and crucially **no `<meta name="robots" content="noindex">`**. Its title is "Northview Psychology — AI Receptionist" — a client demo page that should not be indexed on trivox.ca. Google can and likely will crawl and index it.
- **Fix:** Add `<meta name="robots" content="noindex, nofollow">` to `clinic-chatbot.html`.

**Issue 2b (MINOR) — `workflow-automation.html` LD+JSON `offers.url` has stale Calendly URL:**
The Schema.org `Service` block still points to `https://calendly.com/trivoxinternal/intro-call` while every other page uses `https://cal.com/trivox-interno-hfbm3x/trivox-discovery-call`.
- **Fix:** Update the `offers.url` in `workflow-automation.html` LD+JSON.

---

## 3. Accessibility Basics

**Status: ✅ Clean**

- All pages have `<html lang="en">` ✓
- All indexed pages have exactly one `<h1>` ✓ (clinic-chatbot is a widget, not a document page)
- All `<img>` tags have non-empty `alt` attributes ✓ (trivox-icon "TRIVOX AI logo", sign.jpg has descriptive alt)
- No obviously skipped heading levels found ✓

---

## 4. Hidden-Content / Mobile Bugs

**Status: ✅ Clean**

`[data-reveal]` elements use `opacity:0` + `translateY(26px)` via `motion.css`. The IntersectionObserver in `motion.js` fires at `threshold: 0.15` with `rootMargin: '0px 0px -8% 0px'` — low enough that tall sections on mobile won't get stuck hidden. The GSAP scroll triggers on index.html operate on `transform` only (comment in source explicitly states opacity is handled by the IO, ensuring no element can get stuck invisible). The `.vault` section uses `will-change` only (not `opacity:0`). **No high-threshold risks found.**

---

## 5. Forms

**Status: ✅ Clean**

No `<form>` elements found on any page. CTAs use direct links to cal.com and Stripe. Nothing to audit.

---

## 6. Internal Links

**Status: ❌ Critical — 2 missing pages**

**Issue 6a (CRITICAL) — `what-is-geo.html` does not exist in the repo:**
Referenced as a live link from **5 pages**:
- `index.html` (FAQ answer)
- `ai-automation-toronto.html` (ledger row)
- `ai-automation-gta.html` (ledger row)
- `ai-automation-mississauga.html` (ledger row)
- `ai-receptionist.html` (body copy)

Every click on these links returns a **404** on the live site.

**Issue 6b (CRITICAL) — `get-recommended-by-chatgpt.html` does not exist in the repo:**
Referenced as a live link from the same **5 pages** as above. Same result: **404** on every click.

**Fix:** Either create both pages or remove/replace these links.

**Issue 6c (MINOR) — LinkedIn URL inconsistency:**
- `index.html` footer: `https://www.linkedin.com/company/trivox-ai` (company page)
- `ai-automation-toronto.html`, `ai-voice-agents.html`, `workflow-automation.html` footers: `https://www.linkedin.com/in/trivoxai` (personal/profile URL)

One of these is wrong. Standardize to the correct one across all pages.

**In-page anchors (index.html):** `#services`, `#work`, `#store`, `#about`, `#faq` — all have matching `id` attributes. ✓

**tel: / mailto:** `mailto:info@trivox.ca` correctly formed. ✓

---

## 7. Liveness Check

Liveness: not checked (remote egress restricted — source audit is the primary deliverable).

---

## Cognizance Note

Cognizance: source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).

---

*Generated by TRIVOX Ops QA Agent · 2026-09-18*
