# TRIVOX QA Report — 2026-08-29

**Auditor:** TRIVOX AI QA Agent (automated daily run)  
**Repo:** trivoxinternal-ops/Website → https://trivox.ca  
**Pages audited:** 11 (index.html, ai-automation-toronto.html, ai-automation-gta.html, ai-automation-mississauga.html, ai-receptionist.html, ai-voice-agents.html, workflow-automation.html, case-car-deals-direct.html, case-clinic-receptionist.html, clinic-chatbot.html, privacy.html)  
**Overall status:** ⚠️ 3 issues (1 critical, 2 minor)

---

## 🔴 Critical — Broken Internal Links (Live 404s)

**Affected pages:** index.html, ai-receptionist.html, ai-automation-toronto.html, ai-automation-gta.html, ai-automation-mississauga.html

Two pages are linked from navigation/body copy but **do not exist** in the repo (and therefore return 404 on the live site):

| Missing file | Referenced as |
|---|---|
| `get-recommended-by-chatgpt.html` | "Get Recommended by ChatGPT" nav row + FAQ link |
| `what-is-geo.html` | "What is GEO?" nav row + body link |

These appear in the "More from TRIVOX" link lists on all three location pages and in in-body prose on `ai-receptionist.html` and the homepage FAQ. Any visitor clicking those links lands on a 404.

**Fix:** Either create the two content pages, or temporarily remove the links until the pages are ready.

---

## 🟡 Minor — clinic-chatbot.html: Incomplete SEO / Accessibility

This page appears to be a client-facing chatbot demo (Northview Psychology). It is missing:
- No `<h1>` element (accessibility + SEO)
- No `<meta name="description">`
- No canonical `<link>`
- No `og:image`, `og:title`, `og:description`
- No `twitter:image` / `twitter:card`

If this page is intentionally not indexed, add `<meta name="robots" content="noindex">` and accept the missing SEO tags. If it should be indexed, add the standard meta block.

---

## 🟡 Minor — privacy.html: Missing Social Meta

`privacy.html` is missing:
- `<meta name="description">`
- Canonical `<link>`
- All `og:` and `twitter:` tags

Low SEO risk (privacy pages rarely rank), but social shares will render poorly. Add a minimal meta block.

---

## ✅ Clean Categories

### 1. Broken / Missing Asset References — CLEAN
All local `<img src>`, `<link href>`, `<script src>`, CSS `url()`, and `og:image` references resolve to files that exist in the repo.  
- `assets/favicon.png`, `assets/apple-touch-icon.png`, `assets/og-image.png` ✓  
- `ds/site.css`, `ds/tokens.css`, `ds/v2.css`, `ds/motion.css` ✓  
- `ds/motion.js`, `ds/scroll.js`, `ds/viz.js` ✓  
- `ds/sign.jpg`, `ds/trivox-icon.png`, `ds/trivox-mark.png` ✓

### 2. SEO / Social Meta — CLEAN (except clinic-chatbot.html & privacy.html above)
All 9 main pages have: `<title>`, `<meta name="description">`, canonical `<link>`, `og:title`, `og:description`, `og:image`, `twitter:card`, `twitter:image`, viewport meta, and favicon. `og:image` resolves to `assets/og-image.png` which exists.

### 3. Accessibility Basics — CLEAN
- Every `<img>` has a non-empty `alt` attribute ✓  
- All pages have `<html lang="en">` ✓  
- All main pages have exactly one `<h1>` ✓  
- No obviously skipped heading levels detected

### 4. Hidden-Content / Mobile Bugs — CLEAN
- `data-reveal` elements use `IntersectionObserver` with **threshold: 0.15** (15%) — well below the risky ≥0.4 zone. Tall elements on mobile will reliably intersect and reveal. ✓  
- `motion.css` has a CSS-level `prefers-reduced-motion` fallback that forces `opacity:1 !important` — elements cannot stay hidden if JS fails or reduced-motion is on. ✓  
- `scroll.js` (GSAP ScrollTrigger) is loaded **after** `gsap.min.js` on all pages. ✓  
- `scroll.js` only animates `transform` (position), never `opacity` — worst-case a GSAP misfire means an element doesn't slide, not that it stays invisible. ✓

### 5. Forms — CLEAN
Only one interactive input found (`clinic-chatbot.html` chatbot input, ID `chatInput`) — it's a JS-driven chat widget, not a `<form>` submission, which is correct.

### 6. Internal Anchor Links — CLEAN
All `href="#..."` in-page anchor links resolve to a matching `id` on the same page across all 11 pages. No dangling anchors.

`tel:` and `mailto:` links are well-formed (`mailto:daniel@trivox.ca`, `mailto:info@trivox.ca`, pre-filled subject/body variant).

---

## Liveness
Not checked (remote egress blocked in this environment).

---

## Cognizance
Source audit unavailable (no repo connected — ask Daniel to push cognizance-website to GitHub to include it).

---

*Generated by TRIVOX AI QA Agent · 2026-08-29*
